spring:
  application:
    name: backend
  datasource:
    url: jdbc:h2:mem:mobidb;DB_CLOSE_ON_EXIT=FALSE
    username: mobiuser
    password: mobiPassword!
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true