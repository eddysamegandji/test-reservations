spring:
  application:
    name: backend
  datasource:
    url: jdbc:h2:mem:mobidb
    username: mobiuser
    password: mobiPassword!
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true